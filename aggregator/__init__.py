"""ctf-brain aggregator package.

A small FastAPI daemon that collects live context from a CTF/pentest workstation
(tmux panes, the browser, on-demand screenshots, app logs) and exposes it to an
LLM chat UI. See README.md for the full architecture.
"""

__version__ = "0.1.0"
