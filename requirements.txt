# ctf-brain aggregator. The collectors (tmux_poll, tail) are stdlib-only.
fastapi>=0.110
uvicorn[standard]>=0.27

# LLM providers — install at least the one(s) you use.
anthropic>=0.40
openai>=1.40           # also covers OpenAI-compatible endpoints via OPENAI_BASE_URL

# Optional: Burp-level intercepting proxy feed (proxy/ctf_addon.py).
# The browser extension already captures request/response bodies without this.
# mitmproxy>=10

# dev / tests
pytest>=8.0
httpx>=0.27          # used by fastapi's TestClient
